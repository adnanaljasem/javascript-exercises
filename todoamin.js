let todoItem = {
  name: "buy eggs",
  isDome: false,
};

let todoItems = [];

function todoItem(name) {}

function showItems() {
  for (let i = 0; i < todoItems.length; i++) {
    let todItem = todoItems[i];
    console.log(`id: ${todoItem.id} name: ${todoItem.name}
        , isDone: ${todoItems.isDone}`);
  }
}
function sortItems() {}
